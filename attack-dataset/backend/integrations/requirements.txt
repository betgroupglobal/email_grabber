# Core dependencies
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0
pydantic-settings==2.1.0

# Configuration
pyyaml==6.0.1
python-dotenv==1.0.0

# Vault integration (optional)
hvac==2.1.0

# Docker SDK
docker==6.1.3

# Async HTTP
aiohttp==3.9.1

# Redis (for execution queue)
redis[hiredis]==5.0.1

# Shared libraries (imported from parent directory)
# These will be available via sys.path manipulation